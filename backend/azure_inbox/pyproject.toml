[project]
name = "inbox"
version = "0.1.0"
description = "Wrapper for Microsoft "
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "azure-identity>=1.25.3",
    "dotenv>=0.9.9",
    "msgraph-sdk>=1.62.0",
]
